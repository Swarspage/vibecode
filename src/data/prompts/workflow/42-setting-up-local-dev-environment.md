42 — SETTING UP A LOCAL DEVELOPMENT ENVIRONMENT

Help me configure a new local development environment for this codebase from scratch.

First read the README, package manifests, lockfiles, and CI config.

Then produce:

1) required languages, runtimes, and package managers with versions,
2) dependencies that must be installed globally vs per-project,
3) required environment variables and how to obtain secrets,
4) database setup and seed data,
5) services such as Redis, queues, or third-party emulators,
6) the exact commands to install, migrate, seed, and run,
7) how to run tests and linters,
8) common setup pitfalls and their solutions.

If anything is undocumented or ambiguous, flag it clearly rather than guessing.